apiVersion: v1
kind: Service
metadata:
  name: netalertx
  namespace: netalertx
spec:
  type: ClusterIP
  selector:
    app: netalertx
  ports:
    - name: http
      port: 80
      targetPort: 20211
